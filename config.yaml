# BuildBureau Configuration
# Multi-layered AI Agent System Configuration

# Agent Hierarchy Configuration
agents:
  president:
    count: 1
    model: "gemini-2.0-flash-exp"
    instruction: |
      あなたは社長としてプロジェクト全体を俯瞰し方針を決定する立場です。
      クライアントからの要求を理解し、プロジェクト全体の計画を立案してください。
      部長層へタスクを適切に振り分け、全体の進捗を管理してください。
    allowTools: true
    tools:
      - web_search
      - knowledge_base
    timeout: 120
    retryCount: 3

  president_secretary:
    count: 1
    model: "gemini-2.0-flash-exp"
    instruction: |
      あなたは社長の秘書です。社長の指示を受けて要件を記録し、
      社内ナレッジベースを更新してください。タスクをより詳細に書き出し、
      次の部長秘書に引き継ぎます。
    allowTools: true
    tools:
      - knowledge_base
      - document_manager
    timeout: 60
    retryCount: 3

  department_manager:
    count: 1
    model: "gemini-2.0-flash-exp"
    instruction: |
      あなたは部長としてプロジェクト全体を課長単位に分割する責任者です。
      それぞれの課題に対して担当課長を割り当て、課長秘書にタスクと目標を伝達します。
    allowTools: true
    tools:
      - web_search
      - knowledge_base
    timeout: 120
    retryCount: 3

  department_secretary:
    count: 1
    model: "gemini-2.0-flash-exp"
    instruction: |
      あなたは部長の秘書です。受け取ったタスクをさらに詳細化し、
      関連リサーチ等を実施してください。準備が整ったら部長へタスク指示を渡します。
    allowTools: true
    tools:
      - web_search
      - knowledge_base
      - document_manager
    timeout: 60
    retryCount: 3

  section_manager:
    count: 3
    model: "gemini-2.0-flash-exp"
    instruction: |
      あなたは課長として詳細な実装計画と最終仕様書を策定する責任者です。
      これを複数の平社員エージェントに展開し、実装タスクを割り振ります。
    allowTools: true
    tools:
      - code_analyzer
      - knowledge_base
    timeout: 90
    retryCount: 3

  section_secretary:
    count: 3
    model: "gemini-2.0-flash-exp"
    instruction: |
      あなたは課長の秘書です。割り振られた課題について仕様を詰め、
      必要に応じて他の課長秘書や部長と調整します。
      具体的な実装手順や仕様書のドラフトを作成します。
    allowTools: true
    tools:
      - knowledge_base
      - document_manager
    timeout: 60
    retryCount: 3

  employee:
    count: 6
    model: "gemini-2.0-flash-exp"
    instruction: |
      あなたは与えられた仕様に基づき実装を行うエンジニアです。
      課長から与えられた具体仕様に沿って実装を行い、
      コードやドキュメントなど成果物を作成してください。
    allowTools: true
    tools:
      - code_execution
      - file_operations
      - knowledge_base
    timeout: 180
    retryCount: 3

# LLM Configuration
llm:
  provider: "google"
  apiEndpoint: "https://generativelanguage.googleapis.com"
  defaultModel: "gemini-2.0-flash-exp"
  maxTokens: 8192
  temperature: 0.7
  topP: 0.95

# gRPC Configuration
grpc:
  port: 50051
  maxMessageSize: 10485760  # 10MB
  timeout: 300  # seconds
  enableReflection: true

# Slack Integration
slack:
  enabled: false  # Set to true and provide tokens to enable
  token: "${SLACK_BOT_TOKEN}"
  channelID: "${SLACK_CHANNEL_ID}"
  notifications:
    projectStart:
      enabled: true
      message: "🚀 プロジェクト「{{.ProjectName}}」が開始されました"
    taskComplete:
      enabled: true
      message: "✅ タスク「{{.TaskName}}」が完了しました ({{.Agent}})"
    error:
      enabled: true
      message: "❌ エラーが発生しました: {{.ErrorMessage}} ({{.Agent}})"
    projectComplete:
      enabled: true
      message: "🎉 プロジェクト「{{.ProjectName}}」が完了しました！"
  retryCount: 3
  timeout: 10

# Terminal UI Configuration
ui:
  enableTUI: true
  refreshRate: 100  # milliseconds
  theme: "default"
  showProgress: true
  logLevel: "info"

# System Configuration
system:
  workDir: "./work"
  logDir: "./logs"
  cacheDir: "./cache"
  maxConcurrentTasks: 10
  globalTimeout: 3600  # seconds
